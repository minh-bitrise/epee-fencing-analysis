ultralytics>=8.0.0
opencv-python-headless>=4.8.0
matplotlib>=3.7.0
numpy>=1.24.0
mediapipe>=0.10.0
anthropic>=0.92.0
# backend (assisted-annotation API)
fastapi
uvicorn[standard]
# Multipart form parsing, needed by the upload endpoint. FastAPI does not
# pull it in itself, and without it an upload fails at request-parse time.
python-multipart
